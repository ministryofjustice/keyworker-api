spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:local-public-key.pub

prison:
  uri:
    root: http://localhost:8081
auth:
  uri:
    root: http://localhost:9090/auth